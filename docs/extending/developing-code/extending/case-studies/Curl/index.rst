Adding Builtin Function ``Curl``
================================

Having added the built-in function `KroneckerProduct``, we move onto another built-in function, ``Curl``.

Below are the steps in order that were used to create ``Curl``:


.. toctree::
   :maxdepth: 1

   where
   finding
   skeletal
